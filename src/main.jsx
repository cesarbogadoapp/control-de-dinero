import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import MoneyControlApp from './MoneyControlApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MoneyControlApp />
  </React.StrictMode>
);
